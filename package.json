{
  "name": "rails_tu55",
  "private": true,
  "dependencies": {
    "bootstrap-sass": "^3.3.7"
  },
    "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend_install": "yarn install --modules-folder ./node_modules/"
  }
}
